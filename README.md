# StudentmanagmentP
Built the Student Management System per the Zest India IT assignment spec — a 3-project layered solution:  Core — Student entity, DTOs (with validation attributes), repository/service/token interfaces, custom exceptions Infrastructure — EF Core AppDbContext (SQL Server), StudentRepository, StudentService (handles duplicate-email checks)
